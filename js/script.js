function Monster(name, type, health, strength) {
    this.name = name;
    this.type = type;
    this.health = health;
    this.strength = strength;
}

function getrandomInt(min, max)
return Math.floor(Math.random() * (max - min + 1))
{
    const ghost = new Monster ('Ghost', 'Spirit', 0, 0);
    const whitch = new Monster ('Witch', 'Creature', 0, 0)
    const vampire = new Monster ('Vampire, Creature', 0, 0)
}

function displayMonsters() {
    ghost.health = getRandomInt (20,100);
    ghost.strength = getRandomInt (20,100)
}